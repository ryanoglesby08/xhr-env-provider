{
  "name": "xhr-env-provider",
  "version": "0.0.1",
  "description": "Load client-side configuration from a remote server with an XMLHttpRequest",
  "main": "index.js",
  "scripts": {
    "build": "babel src/EnvProvider.js -o dist/EnvProvider.js",
    "lint": "eslint src/ test/",
    "test": "mocha --require babel-register",
    "precommit": "npm run lint && npm run test && npm run build"
  },
  "author": "Ryan Oglesby <ryan.oglesby08@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0",
    "chai": "^3.5.0",
    "eslint": "^3.9.0",
    "mocha": "^3.1.2",
    "nock": "^9.0.2"
  },
  "dependencies": {
    "es6-promise": "^4.0.5",
    "isomorphic-fetch": "^2.2.1"
  }
}
